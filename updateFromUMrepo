# Update this repo with the latest changes from the (private) TOPPE repo at the U of Michigan fMRI lab

cd +toppe
rsync -avz ~/gitlab/toppe/+toppe/ .
cd ..

cd pulseq
rsync -avz ~/gitlab/toppe/pulseq/ .
cd ..

cd UserGuide
rsync -avz ~/gitlab/toppe/UserGuide/ .
cd ..

# git add <all changed/new files>; git commit; git push;
